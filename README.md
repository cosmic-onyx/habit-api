# Habit API
### API - предоставляющее работу с привычками для телеграмм бота

# Установка и запуск
1. склонируйте репозиторий
```bash
git clone https://github.com/cosmic-onyx/habit-api.git
```
2. скопируйте переменные окружения в .env-example и вставьте в .env (нужно создать новый), или подставьте свои параметры
3. Запустите проект
```bash
docker compose up -d --build
```
## 📚 Документация API

### Базовый URL

```
http://localhost:8000/api/v1
```

### Аутентификация

API использует JWT (JSON Web Tokens) для аутентификации. Все защищенные endpoints требуют заголовок:

```
Authorization: Bearer <access_token>
```

### Endpoints

#### 🔑 Аутентификация

##### Получение JWT токена

**POST** `/api/v1/auth/token/`

Получение access и refresh токенов по Telegram ID и паролю.

**Request Body:**
```json
{
  "telegram_id": "123456789",
  "password": "your_password"
}
```

**Response 200:**
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

##### Обновление токена

**POST** `/api/v1/auth/token/refresh/`

Обновление access токена с помощью refresh токена.

**Request Body:**
```json
{
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

**Response 200:**
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

#### 👤 Пользователи

##### Создание пользователя

**POST** `/api/v1/users/`

Создание нового пользователя.

**Request Body:**
```json
{
  "telegram_id": "123456789",
  "username": "john_doe",
  "first_name": "John",
  "last_name": "Doe",
  "password": "secure_password"
}
```

**Response 201:**
```json
{
  "id": 1,
  "telegram_id": "123456789",
  "username": "john_doe",
  "first_name": "John",
  "last_name": "Doe",
  "date_joined": "2025-11-16T19:00:00Z"
}
```

##### Список пользователей

**GET** `/api/v1/users/`

Получение списка пользователей (с фильтрацией и поиском).

**Query Parameters:**
- `search` - поиск по `telegram_id` и `username`
- `telegram_id` - фильтр по Telegram ID
- `username` - фильтр по username
- `ordering` - сортировка (например: `?ordering=date_joined`)
- `page` - номер страницы
- `page_size` - размер страницы

**Response 200:**
```json
{
  "count": 10,
  "next": "http://localhost:8000/api/v1/users/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "telegram_id": "123456789",
      "username": "john_doe",
      "first_name": "John",
      "last_name": "Doe",
      "date_joined": "2025-11-16T19:00:00Z"
    }
  ]
}
```

##### Получение пользователя

**GET** `/api/v1/users/{id}/`

**Response 200:**
```json
{
  "id": 1,
  "telegram_id": "123456789",
  "username": "john_doe",
  "first_name": "John",
  "last_name": "Doe",
  "date_joined": "2025-11-16T19:00:00Z"
}
```

##### Обновление пользователя

**PUT** `/api/v1/users/{id}/` - полное обновление
**PATCH** `/api/v1/users/{id}/` - частичное обновление

##### Удаление пользователя

**DELETE** `/api/v1/users/{id}/`

#### 📝 Привычки

##### Создание привычки

**POST** `/api/v1/habits/`

**Request Body:**
```json
{
  "title": "Утренняя зарядка",
  "repeat": "ежедневно",
  "execution_time": "07:00:00"
}
```

**Response 201:**
```json
{
  "id": 1,
  "title": "Утренняя зарядка",
  "repeat": "ежедневно",
  "execution_time": "07:00:00",
  "created_at": "2025-11-16T19:00:00Z",
  "updated_at": "2025-11-16T19:00:00Z"
}
```

##### Список привычек

**GET** `/api/v1/habits/`

Возвращает только привычки текущего пользователя.

**Query Parameters:**
- `ordering` - сортировка (например: `?ordering=-created_at`)
- `page` - номер страницы
- `page_size` - размер страницы

**Response 200:**
```json
{
  "count": 5,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 1,
      "title": "Утренняя зарядка",
      "repeat": "ежедневно",
      "execution_time": "07:00:00",
      "created_at": "2025-11-16T19:00:00Z",
      "updated_at": "2025-11-16T19:00:00Z"
    }
  ]
}
```

##### Получение привычки

**GET** `/api/v1/habits/{id}/`

##### Обновление привычки

**PUT** `/api/v1/habits/{id}/` - полное обновление
**PATCH** `/api/v1/habits/{id}/` - частичное обновление

##### Удаление привычки

**DELETE** `/api/v1/habits/{id}/`

#### 📊 Журнал привычек

##### Создание записи в журнале

**POST** `/api/v1/habits/{habit_id}/logs/`

**Request Body:**
```json
{
  "is_done": "сделано"
}
```

**Response 201:**
```json
{
  "id": 1,
  "habit": {
    "id": 1,
    "title": "Утренняя зарядка",
    "repeat": "ежедневно",
    "execution_time": "07:00:00",
    "created_at": "2025-11-16T19:00:00Z",
    "updated_at": "2025-11-16T19:00:00Z"
  },
  "is_done": "сделано",
  "created_at": "2025-11-16T20:00:00Z"
}
```

##### Список записей журнала

**GET** `/api/v1/habits/{habit_id}/logs/`

**Query Parameters:**
- `is_done` - фильтр по статусу
- `search` - поиск по статусу
- `ordering` - сортировка
- `page` - номер страницы
- `page_size` - размер страницы

**Response 200:**
```json
{
  "count": 10,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 1,
      "habit": {
        "id": 1,
        "title": "Утренняя зарядка",
        "repeat": "ежедневно",
        "execution_time": "07:00:00",
        "created_at": "2025-11-16T19:00:00Z",
        "updated_at": "2025-11-16T19:00:00Z"
      },
      "is_done": "сделано",
      "created_at": "2025-11-16T20:00:00Z"
    }
  ]
}
```

##### Получение записи журнала

**GET** `/api/v1/habits/{habit_id}/logs/{id}/`

##### Обновление записи журнала

**PUT** `/api/v1/habits/{habit_id}/logs/{id}/` - полное обновление
**PATCH** `/api/v1/habits/{habit_id}/logs/{id}/` - частичное обновление

##### Удаление записи журнала

**DELETE** `/api/v1/habits/{habit_id}/logs/{id}/`

### Celery задачи

#### `select_today_habits`
Периодическая задача (запускается по расписанию), которая:
- Находит привычки, для которых нужно отправить напоминание
- Запускает задачи отправки напоминаний для каждой привычки

#### `remind_of_habit`
Задача отправки напоминания:
- Получает ID привычки
- Загружает данные привычки и пользователя
- Отправляет напоминание через Telegram

### Настройки безопасности

- **Throttling**: 
  - Анонимные пользователи: 100 запросов/день
  - Авторизованные пользователи: 1000 запросов/минуту
  - Привычки API: 15 запросов/минуту

- **Аутентификация**: JWT токены с временем жизни 7 дней

- **Разрешения**: 
  - Публичные endpoints: создание пользователя, получение токена
  - Защищенные endpoints: все операции с привычками требуют аутентификации

**Примечание**: Для работы с Telegram-ботом необходимо настроить `TELEGRAM_BOT_TOKEN` в переменных окружения.


